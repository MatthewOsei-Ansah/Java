public class BinaryTree<T> {
    private Node<T> Root;
    private int NumofNodes = 0;

    public void addnode(T value){
        Node<T> current;
        int compare;
        Node<T> newValue = new Node<>(value, )
        
        if(Root != null){
            current = Root;
            compare = Root.compareTo();
            if()
        }
    }
}
